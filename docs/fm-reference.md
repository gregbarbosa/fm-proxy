# `fm` — Apple Foundation Models CLI: Complete Reference

_Auto-generated by `tools/gen-fm-docs.py` from `fm --experimental-dump-help`. Do not edit by hand._


## `fm`

A command-line interface for Apple Foundation Models.


| Option | Description |
|---|---|
| `-h, --help` | Show help information. |

## `fm respond`

Generate a response to a prompt.


```
MODEL OPTIONS:
  --model system     - On-device Apple Foundation Model (default)
  --model pcc        - Apple Foundation Model on Private Cloud Compute
```


| Option | Description |
|---|---|
| `--stream, --no-stream` | Stream the output as it's generated _(default: `--stream`)_ |
| `--greedy, -g` | Use greedy sampling |
| `--model, -m <model>` | Model to use: system or pcc _(default: `system`)_ |
| `--use-case <use-case>` | Use case for the language model |
| `--guardrails <guardrails>` | Guardrail level for the language model |
| `--verbose, -v` | Print verbose output |
| `--instructions, -i <instructions>` | Instructions for the model to follow |
| `--schema <schema>` | A path to a schema file, or an inline schema string |
| `<prompt>` | A prompt for the model to respond to |
| `--text <text>` | Text segment to include in the prompt |
| `--image <image>` | Path to an image to include in the prompt |
| `--load-transcript <load-transcript>` | Path to a saved transcript for conversational context |
| `--save-transcript <save-transcript>` | Save the session transcript to ~/.fm/sessions/<name> after responding |
| `-h, --help` | Show help information. |

## `fm chat`

Start an interactive chat session.


```
Start an interactive multi-turn conversation. Sessions are automatically saved to ~/.fm/sessions/ and can be resumed later with --resume.
```


| Option | Description |
|---|---|
| `--model, -m <model>` | Model to use: system or pcc _(default: `system`)_ |
| `--use-case <use-case>` | Use case for the language model |
| `--guardrails <guardrails>` | Guardrail level for the language model |
| `--resume, -r <resume>` | Resume a saved chat session |
| `--continue` | Continue the most recent chat session |
| `--instructions, -i <instructions>` | Instructions for the model |
| `--set-default-model <set-default-model>` | Persist <model> as the default for future fm chat sessions |
| `-h, --help` | Show help information. |

## `fm token-count`

Count the tokens in a prompt, instructions, or saved transcript.


```
Count the number of tokens the request would consume using the on-device system model's tokenizer. Supports positional prompts, --text and --image segments, --instructions, and --load-transcript.

When stdout is a terminal the output is prefixed with 'Token count: '; when piped or redirected (or when --quiet is passed) only the integer is printed. When no prompt is given on the command line, the prompt is read from standard input if stdin is piped.
```


| Option | Description |
|---|---|
| `--verbose, -v` | Print verbose output |
| `--instructions, -i <instructions>` | Instructions for the model to follow |
| `--schema <schema>` | A path to a schema file, or an inline schema string |
| `<prompt>` | A prompt for the model to respond to |
| `--text <text>` | Text segment to include in the prompt |
| `--image <image>` | Path to an image to include in the prompt |
| `--load-transcript <load-transcript>` | Path to a saved transcript for conversational context |
| `--quiet, -q` | Print only the integer count (automatic when stdout is piped or redirected) |
| `-h, --help` | Show help information. |

## `fm schema`

Generate a structured output generation schema.


| Option | Description |
|---|---|
| `-h, --help` | Show help information. |

## `fm schema object`

Generate a structured output schema for an object type.


```
Declares properties sequentially. Each --string, --integer, --double,
or --boolean starts a new property declaration. Follow with
--description, --optional, and/or --array to annotate the property.

  --string <name>       Declare a string property.
  --integer <name>      Declare an integer property.
  --double <name>       Declare a floating-point property.
  --boolean <name>      Declare a boolean property.
  --nested <name>       Declare a property with a nested schema (must be followed by --schema).
  --schema <json>       Provide a structured output schema (after --nested, or after --anyOf for union choices).
  --anyOf               Build an anyOf union schema. Subsequent --schema args become choices.
  --array               Mark the preceding property as an array of that type.
  --description <text>  Set description on the preceding property.
  --optional            Mark the preceding property as optional.

Use dot notation (a.b) in property names to create nested objects.
For example, 'address.street' and 'address.zip' produce an 'Address'
object with 'street' and 'zip' properties.
```


| Option | Description |
|---|---|
| `--name <name>` | Name of the root object type. |
| `<property-declarations>` |  |
| `-h, --help` | Show help information. |

## `fm serve`

Start a Chat Completions API server for Foundation Models.


```
Starts an HTTP server that exposes the Chat Completions API. Any client that supports the Chat Completions API can connect to it. Specify the model per request via the "model" field: "system" (default) or "pcc".

Use --socket for Unix domain socket transport (recommended for local Python bindings). Use --host/--port for TCP (default).

ENDPOINTS:
  POST /v1/chat/completions  - Chat completions (streaming & non-streaming)
  GET  /v1/models            - List available models
  GET  /health               - Health check
```


| Option | Description |
|---|---|
| `--host <host>` | Host address to bind to (TCP mode) |
| `--port <port>` | Port to listen on, 1-65535 (TCP mode) |
| `--socket <socket>` | Unix domain socket path (socket mode) |
| `-h, --help` | Show help information. |

## `fm available`

Check model availability.


```
Check whether the specified model is available. If no model is specified, all models are checked.
```


| Option | Description |
|---|---|
| `--model, -m <model>` | Model to check: system or pcc (default: all) |
| `--use-case <use-case>` | Use case for the language model |
| `--guardrails <guardrails>` | Guardrail level for the language model |
| `-h, --help` | Show help information. |

## `fm quota-usage`

Check model quota usage.


```
Fetches the current quota usage for the specified model. If no model is specified, all models are checked. Quota applies only to the Private Cloud Compute model; the on-device system model reports as not applicable.
```


| Option | Description |
|---|---|
| `--model, -m <model>` | Model to check: system or pcc (default: all) |
| `-h, --help` | Show help information. |
